# 1.HelloWindow
* Create a Window using SDL
* Outline the typical SDL + OpenGL program structure
* Bring in GLAD library to Common directory in the root directory of the repo
* Create build environment that works with OpenGL
* Use OpenGL to query device + library info

